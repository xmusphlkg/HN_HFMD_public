# Data dictionary

`synthetic_region` is a fictional label; `year` and `iso_week` are aggregate time indices; counts are deliberately bounded at ten or more; `ev_a71_vaccine_proxy` is a generated sigmoid exposure.
